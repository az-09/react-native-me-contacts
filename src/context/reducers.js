

const reducers = (state, {type, payload}) => {
    switch(type){
        case 'LOGIN':
            return state

        case 'GET_CONTACTS':
            return state
        default:
            return state
    }
}

export default reducers